<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.ActualizarRecursoAzulPage"
             BackgroundColor="#f8f9fa"
             Title="Editar Recurso">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Editar Recurso"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="ID: REC-101 | Última modificación: 15/05/2025"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Formulario -->
            <Frame BackgroundColor="White" CornerRadius="10" Padding="20" HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- Título -->
                    <VerticalStackLayout>
                        <Label Text="Título *" FontAttributes="Bold"/>
                        <Entry Text="Guía Completa de HTML5"/>
                    </VerticalStackLayout>

                    <!-- Descripción -->
                    <VerticalStackLayout>
                        <Label Text="Descripción *" FontAttributes="Bold"/>
                        <Editor Text="Este recurso contiene todos los elementos de HTML5 con ejemplos prácticos."
                                AutoSize="TextChanges"
                                HeightRequest="100"/>
                    </VerticalStackLayout>

                    <!-- Tipo y Materia -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout>
                            <Label Text="Tipo *" FontAttributes="Bold"/>
                            <Picker Title="Seleccionar..." SelectedIndex="1">
                                <Picker.Items>
                                    <x:String>Documento PDF</x:String>
                                    <x:String>Video</x:String>
                                    <x:String>Enlace Web</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <Label Text="Materia *" FontAttributes="Bold"/>
                            <Picker Title="Seleccionar..." SelectedIndex="1">
                                <Picker.Items>
                                    <x:String>Programación Básica</x:String>
                                    <x:String>Diseño Web</x:String>
                                    <x:String>Bases de Datos</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Archivo Actual -->
                    <VerticalStackLayout>
                        <Label Text="Archivo Actual" FontAttributes="Bold"/>
                        <Frame BackgroundColor="#f8f9fa" CornerRadius="5" Padding="10">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">
                                <VerticalStackLayout>
                                    <Label Text="📄 guia_html5.mp4"/>
                                    <Label Text="Subido el 10/05/2025"
                                           FontSize="12"
                                           TextColor="Gray"/>
                                </VerticalStackLayout>
                                <Button Text="🗑"
                                        Grid.Column="1"
                                        BackgroundColor="#d93025"
                                        TextColor="White"
                                        CornerRadius="5"
                                        HeightRequest="30"
                                        WidthRequest="30"/>
                            </Grid>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Reemplazar archivo -->
                    <VerticalStackLayout>
                        <Label Text="Reemplazar Archivo" FontAttributes="Bold"/>
                        <Frame BorderColor="#dee2e6"
                               BackgroundColor="#ffffff"
                               Padding="20"
                               CornerRadius="5"
                               HasShadow="False">
                            <VerticalStackLayout HorizontalOptions="Center">
                                <Label Text="📁"
                                       FontSize="28"
                                       HorizontalOptions="Center"/>
                                <Label Text="Toca para seleccionar nuevo archivo"
                                       FontSize="14"
                                       HorizontalOptions="Center"/>
                                <Label Text="Tamaño máximo: 25MB"
                                       FontSize="12"
                                       TextColor="Gray"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Botones -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" Margin="0,20,0,0">
                        <Button Text="⬅ Volver"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="10"
                                Grid.Column="0"/>
                        <HorizontalStackLayout Grid.Column="1" HorizontalOptions="End" Spacing="10">
                            <Button Text="🗑 Eliminar"
                                    BackgroundColor="#d93025"
                                    TextColor="White"
                                    CornerRadius="10"/>
                            <Button Text="💾 Actualizar"
                                    BackgroundColor="#1a73e8"
                                    TextColor="White"
                                    CornerRadius="10"/>
                        </HorizontalStackLayout>
                    </Grid>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
