<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.ActualizarRecursoPage"
             BackgroundColor="#f8f9fa"
             Title="Editar Recurso">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Editar Recurso"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="ID: REC-205 | Última modificación: 15/05/2025"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Formulario -->
            <Frame BackgroundColor="White" CornerRadius="10" Padding="20" HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- ID del recurso (solo lectura) -->
                    <VerticalStackLayout>
                        <Label Text="ID del Recurso *" FontAttributes="Bold"/>
                        <Entry Text="REC-205" IsReadOnly="True" BackgroundColor="#f0f0f0"/>
                    </VerticalStackLayout>

                    <!-- Título -->
                    <VerticalStackLayout>
                        <Label Text="Título *" FontAttributes="Bold"/>
                        <Entry Text="Guía Completa de HTML5" Placeholder="Título del recurso"/>
                    </VerticalStackLayout>

                    <!-- Descripción -->
                    <VerticalStackLayout>
                        <Label Text="Descripción *" FontAttributes="Bold"/>
                        <Editor Text="Este recurso contiene todos los elementos de HTML5 con ejemplos prácticos y ejercicios."
                                Placeholder="Describa el contenido..."
                                AutoSize="TextChanges"
                                HeightRequest="100"/>
                    </VerticalStackLayout>

                    <!-- Tipo y Materia -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout>
                            <Label Text="Tipo de Recurso *" FontAttributes="Bold"/>
                            <Picker Title="Seleccionar tipo" SelectedIndex="1">
                                <Picker.Items>
                                    <x:String>Documento PDF</x:String>
                                    <x:String>Video</x:String>
                                    <x:String>Presentación</x:String>
                                    <x:String>Enlace Web</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <Label Text="Materia *" FontAttributes="Bold"/>
                            <Picker Title="Seleccionar materia" SelectedIndex="1">
                                <Picker.Items>
                                    <x:String>Programación Básica</x:String>
                                    <x:String>Diseño Web</x:String>
                                    <x:String>Bases de Datos</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Archivo actual -->
                    <VerticalStackLayout>
                        <Label Text="Archivo Actual" FontAttributes="Bold"/>
                        <Frame BackgroundColor="#f8f9fa" CornerRadius="5" Padding="10">
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="📄" FontSize="18" VerticalOptions="Center"/>
                                <Label Text="video_html5.mp4" VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Checkbox -->
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="True"/>
                        <Label Text="Hacer recurso público"
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <!-- Botones -->
                    <HorizontalStackLayout HorizontalOptions="End" Spacing="15" Margin="0,20,0,0">
                        <Button Text="❌ Cancelar"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="10"/>
                        <Button Text="💾 Guardar Cambios"
                                BackgroundColor="#0b8043"
                                TextColor="White"
                                CornerRadius="10"/>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
