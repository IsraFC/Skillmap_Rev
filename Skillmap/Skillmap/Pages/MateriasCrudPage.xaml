<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.MateriasCrudPage"
             BackgroundColor="#f8f9fa"
             Title="Gestión de Materias">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Título -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <Label Text="Gestión de Materias"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalTextAlignment="Center"/>
            </Frame>

            <!-- Barra de acciones -->
            <HorizontalStackLayout Spacing="15">
                <Button Text="➕ Nueva Materia"
                        BackgroundColor="#1a73e8"
                        TextColor="White"
                        CornerRadius="10"/>
                <Entry Placeholder="Buscar materia..."
                       WidthRequest="200"
                       BackgroundColor="White"/>
                <Button Text="🔍"
                        BackgroundColor="#d63384"
                        TextColor="White"
                        CornerRadius="10"/>
            </HorizontalStackLayout>

            <!-- Lista de materias -->
            <VerticalStackLayout Spacing="15">

                <!-- Materia 1 -->
                <Frame BackgroundColor="White"
                       BorderColor="#1a73e8"
                       CornerRadius="10"
                       Padding="15">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Código: MAT-101" FontAttributes="Bold" TextColor="#1a73e8"/>
                        <Label Text="Nombre: Programación Básica"/>
                        <Label Text="Semestre: 3"/>
                        <Label Text="Docente: Dra. Sánchez"/>
                        <HorizontalStackLayout Spacing="10">
                            <Button Text="✏️ Editar"
                                    BackgroundColor="#1a73e8"
                                    TextColor="White"
                                    CornerRadius="8"/>
                            <Button Text="🗑 Eliminar"
                                    BackgroundColor="#d93025"
                                    TextColor="White"
                                    CornerRadius="8"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <!-- Materia 2 -->
                <Frame BackgroundColor="White"
                       BorderColor="#1a73e8"
                       CornerRadius="10"
                       Padding="15">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Código: MAT-205" FontAttributes="Bold" TextColor="#1a73e8"/>
                        <Label Text="Nombre: Bases de Datos"/>
                        <Label Text="Semestre: 5"/>
                        <Label Text="Docente: Dr. Rodríguez"/>
                        <HorizontalStackLayout Spacing="10">
                            <Button Text="✏️ Editar"
                                    BackgroundColor="#1a73e8"
                                    TextColor="White"
                                    CornerRadius="8"/>
                            <Button Text="🗑 Eliminar"
                                    BackgroundColor="#d93025"
                                    TextColor="White"
                                    CornerRadius="8"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>

            <!-- Paginación -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10" Margin="0,10,0,0">
                <Button Text="Anterior" IsEnabled="False"/>
                <Button Text="1" BackgroundColor="#1a73e8" TextColor="White"/>
                <Button Text="2"/>
                <Button Text="Siguiente"/>
            </HorizontalStackLayout>

            <!-- Crear Nueva Materia -->
            <Frame BackgroundColor="White"
                   BorderColor="#198754"
                   CornerRadius="10"
                   Padding="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <Label Text="📘 Crear Nueva Materia"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#198754"/>

                    <VerticalStackLayout>
                        <Label Text="Código"/>
                        <Entry Placeholder="Ej: MAT-101"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout>
                        <Label Text="Nombre"/>
                        <Entry Placeholder="Ej: Programación Avanzada"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout>
                        <Label Text="Semestre"/>
                        <Picker Title="Selecciona el semestre">
                            <Picker.Items>
                                <x:String>1</x:String>
                                <x:String>2</x:String>
                                <x:String>3</x:String>
                                <x:String>4</x:String>
                                <x:String>5</x:String>
                                <x:String>6</x:String>
                            </Picker.Items>
                        </Picker>
                    </VerticalStackLayout>

                    <VerticalStackLayout>
                        <Label Text="Docente"/>
                        <Picker Title="Selecciona docente">
                            <Picker.Items>
                                <x:String>Dr. Rodríguez</x:String>
                                <x:String>Dra. Sánchez</x:String>
                                <x:String>Mtro. López</x:String>
                            </Picker.Items>
                        </Picker>
                    </VerticalStackLayout>

                    <!-- Botones -->
                    <HorizontalStackLayout HorizontalOptions="End" Spacing="10">
                        <Button Text="Cancelar"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="10"/>
                        <Button Text="Guardar Materia"
                                BackgroundColor="#198754"
                                TextColor="White"
                                CornerRadius="10"/>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
