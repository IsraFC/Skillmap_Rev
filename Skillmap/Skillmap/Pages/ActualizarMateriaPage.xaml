<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.ActualizarMateriaPage"
             BackgroundColor="#f8f9fa"
             Title="Actualizar Materia">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Actualizar Materia"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Código: MAT-101 | Última actualización: 20/05/2025"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Alerta informativa -->
            <Frame BorderColor="#1a73e8" BackgroundColor="#eef3fb" Padding="10" CornerRadius="6">
                <HorizontalStackLayout Spacing="10">
                    <Label Text="ℹ️"
                           FontSize="20"
                           TextColor="#1a73e8"
                           VerticalOptions="Center"/>
                    <Label Text="Editando materia activa - Los cambios afectarán a 45 estudiantes"
                           VerticalOptions="Center"
                           TextColor="Black"
                           FontSize="14"/>
                </HorizontalStackLayout>
            </Frame>

            <!-- Formulario -->
            <Frame BackgroundColor="White" CornerRadius="10" Padding="20" HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- Código y Semestre -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout>
                            <Label Text="Código *" FontAttributes="Bold"/>
                            <Entry Text="MAT-101"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <Label Text="Semestre *" FontAttributes="Bold"/>
                            <Picker Title="Selecciona el semestre" SelectedIndex="2">
                                <Picker.Items>
                                    <x:String>1</x:String>
                                    <x:String>2</x:String>
                                    <x:String>3</x:String>
                                    <x:String>4</x:String>
                                    <x:String>5</x:String>
                                    <x:String>6</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Nombre -->
                    <VerticalStackLayout>
                        <Label Text="Nombre *" FontAttributes="Bold"/>
                        <Entry Text="Programación Básica"/>
                    </VerticalStackLayout>

                    <!-- Docente -->
                    <VerticalStackLayout>
                        <Label Text="Docente *" FontAttributes="Bold"/>
                        <Picker Title="Selecciona docente" SelectedIndex="1">
                            <Picker.Items>
                                <x:String>Dr. Rodríguez</x:String>
                                <x:String>Dra. Sánchez</x:String>
                                <x:String>Mtro. López</x:String>
                            </Picker.Items>
                        </Picker>
                    </VerticalStackLayout>

                    <!-- Checkbox -->
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="True"/>
                        <Label Text="Materia activa" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <!-- Botones -->
                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="10" Margin="0,20,0,0">
                        <!-- Volver -->
                        <Button Text="⬅ Volver al listado"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="10"
                                Grid.Column="0"/>

                        <!-- Acciones -->
                        <HorizontalStackLayout Grid.Column="1" HorizontalOptions="End" Spacing="10">
                            <Button Text="🗑 Desactivar"
                                    BackgroundColor="#d93025"
                                    TextColor="White"
                                    CornerRadius="10"/>
                            <Button Text="💾 Guardar Actualización"
                                    BackgroundColor="#1a73e8"
                                    TextColor="White"
                                    CornerRadius="10"/>
                        </HorizontalStackLayout>
                    </Grid>

                </VerticalStackLayout>
            </Frame>

            <!-- Simulación visual de modal de confirmación -->
            <Frame BackgroundColor="#fff3cd"
                   BorderColor="#ffc107"
                   Padding="15"
                   CornerRadius="8">
                <VerticalStackLayout Spacing="10">
                    <Label Text="⚠️ Confirmar Cambios"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#856404"/>
                    <Label Text="¿Estás seguro de actualizar esta materia?"
                           TextColor="Black"/>
                    <Label Text="Esto afectará a 45 estudiantes matriculados."
                           TextColor="Black"/>

                    <HorizontalStackLayout HorizontalOptions="End" Spacing="10">
                        <Button Text="Cancelar"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="8"/>
                        <Button Text="Confirmar Actualización"
                                BackgroundColor="#1a73e8"
                                TextColor="White"
                                CornerRadius="8"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
