<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.FeedbackRecursosPage"
             Title="Feedback de Recursos"
             BackgroundColor="#f8f9fa">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Encabezado -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="💬 Feedback de Usuarios"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Comentarios recientes sobre recursos educativos"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Feedback Listado -->
            <CollectionView>
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="15"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="White"
                               BorderColor="#dee2e6"
                               CornerRadius="8"
                               Padding="15"
                               HasShadow="True">

                            <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                                <!-- Recurso y Fecha -->
                                <Label Text="📘 Guía HTML5"
                                       FontAttributes="Bold"
                                       Grid.Row="0" Grid.Column="0"/>
                                <Label Text="15/05/2025"
                                       FontSize="12"
                                       TextColor="Gray"
                                       HorizontalOptions="End"
                                       Grid.Row="0" Grid.Column="1"/>

                                <!-- Usuario y calificación -->
                                <Label Text="maria@mail.com"
                                       FontSize="12"
                                       TextColor="Gray"
                                       Grid.Row="1" Grid.Column="0"/>
                                <Label Text="⭐ 5"
                                       FontAttributes="Bold"
                                       TextColor="Green"
                                       HorizontalOptions="End"
                                       Grid.Row="1" Grid.Column="1"/>

                                <!-- Comentario y acción -->
                                <Label Text="Muy útil para principiantes"
                                       Grid.Row="2" Grid.Column="0"
                                       Grid.ColumnSpan="1"/>
                                <Button Text="🗑 Eliminar"
                                        BackgroundColor="#d93025"
                                        TextColor="White"
                                        CornerRadius="6"
                                        HeightRequest="30"
                                        WidthRequest="90"
                                        FontSize="12"
                                        HorizontalOptions="End"
                                        Grid.Row="2"
                                        Grid.Column="1"/>
                            </Grid>

                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
