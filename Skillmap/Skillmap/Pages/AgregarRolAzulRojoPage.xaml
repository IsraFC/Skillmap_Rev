<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.AgregarRolAzulRojoPage"
             BackgroundColor="#f8f9fa"
             Title="Agregar Nuevo Rol">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="🛡 Agregar Nuevo Rol"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Complete los campos requeridos (*)"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Plantillas -->
            <VerticalStackLayout>
                <Label Text="Configuraciones rápidas:"/>
                <HorizontalStackLayout Spacing="10">
                    <Button Text="🛡 Administrador" BackgroundColor="#e9ecef" TextColor="#1a73e8" CornerRadius="20"/>
                    <Button Text="🎓 Docente" BackgroundColor="#e9ecef" TextColor="#1a73e8" CornerRadius="20"/>
                    <Button Text="🎒 Estudiante" BackgroundColor="#e9ecef" TextColor="#1a73e8" CornerRadius="20"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>

            <!-- Datos básicos -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <VerticalStackLayout>
                    <Label Text="Nombre del Rol *"/>
                    <Entry x:Name="nombreRol" Placeholder="Ej: Auditor"/>
                </VerticalStackLayout>
                <VerticalStackLayout>
                    <Label Text="ID Rol"/>
                    <Entry Text="Se generará automáticamente" IsEnabled="False"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Permisos -->
            <VerticalStackLayout>
                <Label Text="Permisos *"/>
                <Frame BorderColor="#dee2e6" BackgroundColor="#f2f2f2" CornerRadius="5" Padding="15">
                    <VerticalStackLayout Spacing="12">
                        <VerticalStackLayout>
                            <CheckBox x:Name="pUsuarios">
                            </CheckBox>
                            <Label Text="Crear, editar y eliminar usuarios" FontSize="12" TextColor="Gray"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <CheckBox x:Name="pMaterias">
                            </CheckBox>
                            <Label Text="Administrar el plan de estudios" FontSize="12" TextColor="Gray"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <CheckBox x:Name="pRecursos" IsChecked="True">
                            </CheckBox>
                            <Label Text="Subir y organizar materiales" FontSize="12" TextColor="Gray"/>
                        </VerticalStackLayout>

                        <CheckBox x:Name="pReportes">
                        </CheckBox>

                        <VerticalStackLayout>
                            <CheckBox x:Name="pConfig">
                            </CheckBox>
                            <Label Text="Ajustes avanzados" FontSize="12" TextColor="Gray"/>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>
                <Label x:Name="errorPermisos" Text="Debe seleccionar al menos un permiso" FontSize="12" TextColor="#d93025" IsVisible="False"/>
            </VerticalStackLayout>

            <!-- Estado del rol -->
            <HorizontalStackLayout Spacing="10">
                <Label Text="Rol activo" VerticalOptions="Center"/>
                <Switch IsToggled="True"/>
            </HorizontalStackLayout>

            <!-- Botones -->
            <HorizontalStackLayout HorizontalOptions="End" Spacing="10" Margin="0,20,0,0">
                <Button Text="❌ Cancelar"
                        BackgroundColor="Gray"
                        TextColor="White"
                        CornerRadius="10"/>
                <Button Text="💾 Guardar Rol"
                        BackgroundColor="#1a73e8"
                        TextColor="White"
                        CornerRadius="10"/>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
