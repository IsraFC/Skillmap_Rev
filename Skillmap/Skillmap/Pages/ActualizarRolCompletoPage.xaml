<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.ActualizarRolCompletoPage"
             BackgroundColor="#f8f9fa"
             Title="Actualizar Rol">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Encabezado -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Actualizar Rol: Coordinador"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="ID: ROL-COORD | Última actualización: 20/05/2025"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Alerta de impacto -->
            <Frame BackgroundColor="#fef2f2" BorderColor="#f5c2c7" CornerRadius="5" Padding="15">
                <HorizontalStackLayout Spacing="10">
                    <Label Text="⚠️" FontSize="20"/>
                    <VerticalStackLayout>
                        <Label Text="¡Atención! Este rol está asignado a 8 usuarios"
                               FontAttributes="Bold"/>
                        <Label Text="Los cambios afectarán sus permisos inmediatamente"
                               FontSize="12"
                               TextColor="Gray"/>
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Frame>

            <!-- Formulario -->
            <Frame BackgroundColor="White" CornerRadius="10" Padding="20" HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- Nombre e ID -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout>
                            <Label Text="Nombre del Rol *"/>
                            <Entry Text="Coordinador"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout>
                            <Label Text="ID Rol"/>
                            <Entry Text="ROL-COORD" IsEnabled="False"/>
                            <Label Text="Identificador único (no editable)"
                                   FontSize="12"
                                   TextColor="Gray"/>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Permisos -->
                    <VerticalStackLayout>
                        <Label Text="Permisos *"/>
                        <Frame BorderColor="#dee2e6" BackgroundColor="#f2f2f2" CornerRadius="5" Padding="15">
                            <VerticalStackLayout Spacing="10">
                                <CheckBox IsChecked="True">
                                </CheckBox>
                                <CheckBox IsChecked="True">
                                </CheckBox>
                                <CheckBox>
                                </CheckBox>
                                <CheckBox IsChecked="True">
                                </CheckBox>
                                <CheckBox>
                                </CheckBox>
                            </VerticalStackLayout>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Estado -->
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Rol activo" VerticalOptions="Center"/>
                        <Switch IsToggled="True"/>
                    </HorizontalStackLayout>

                    <!-- Botones -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" Margin="0,20,0,0">
                        <Button Text="⬅ Volver al listado"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="10"
                                Grid.Column="0"/>
                        <HorizontalStackLayout Grid.Column="1" HorizontalOptions="End" Spacing="10">
                            <Button Text="⛔ Desactivar"
                                    BackgroundColor="#d93025"
                                    TextColor="White"
                                    CornerRadius="10"/>
                            <Button Text="💾 Guardar Cambios"
                                    BackgroundColor="#1a73e8"
                                    TextColor="White"
                                    CornerRadius="10"/>
                        </HorizontalStackLayout>
                    </Grid>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
