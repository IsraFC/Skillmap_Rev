<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.CrearRecursoAzulPage"
             BackgroundColor="#f8f9fa"
             Title="Nuevo Recurso">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Encabezado -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Nuevo Recurso"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Complete los campos requeridos (*)"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Formulario -->
            <Frame BackgroundColor="White" CornerRadius="10" Padding="20" HasShadow="True">
                <VerticalStackLayout Spacing="15">

                    <!-- Título -->
                    <VerticalStackLayout>
                        <Label Text="Título *" FontAttributes="Bold"/>
                        <Entry Placeholder="Ej: Guía de HTML5"/>
                    </VerticalStackLayout>

                    <!-- Descripción -->
                    <VerticalStackLayout>
                        <Label Text="Descripción *" FontAttributes="Bold"/>
                        <Editor Placeholder="Escriba la descripción..." AutoSize="TextChanges" HeightRequest="100"/>
                    </VerticalStackLayout>

                    <!-- Tipo y Materia -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout>
                            <Label Text="Tipo *" FontAttributes="Bold"/>
                            <Picker Title="Seleccionar...">
                                <Picker.Items>
                                    <x:String>Documento PDF</x:String>
                                    <x:String>Video</x:String>
                                    <x:String>Enlace Web</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <Label Text="Materia *" FontAttributes="Bold"/>
                            <Picker Title="Seleccionar...">
                                <Picker.Items>
                                    <x:String>Programación Básica</x:String>
                                    <x:String>Bases de Datos</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Contenido -->
                    <VerticalStackLayout>
                        <Label Text="Contenido *" FontAttributes="Bold"/>
                        <Frame BorderColor="#dee2e6"
                               BackgroundColor="#ffffff"
                               Padding="20"
                               CornerRadius="5"
                               HasShadow="False">
                            <VerticalStackLayout HorizontalOptions="Center">
                                <Label Text="☁️" FontSize="32" HorizontalOptions="Center"/>
                                <Label Text="Arrastre el archivo aquí o toque para seleccionar"
                                       FontSize="14"
                                       HorizontalOptions="Center"/>
                                <Label Text="Tamaño máximo: 25MB"
                                       FontSize="12"
                                       TextColor="Gray"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Botones -->
                    <HorizontalStackLayout HorizontalOptions="End" Spacing="15" Margin="0,20,0,0">
                        <Button Text="❌ Cancelar"
                                BackgroundColor="LightGray"
                                TextColor="Black"
                                CornerRadius="10"/>
                        <Button Text="💾 Guardar"
                                BackgroundColor="#1a73e8"
                                TextColor="White"
                                CornerRadius="10"/>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
