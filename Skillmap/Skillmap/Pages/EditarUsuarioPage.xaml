<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.EditarUsuarioPage"
             BackgroundColor="#f8f9fa"
             Title="Editar Usuario">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="Editar Usuario"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White" />
                    <Label Text="ID: USR-101 | Registrado: 15/05/2025"
                           FontSize="14"
                           TextColor="White" />
                </VerticalStackLayout>
            </Frame>

            <!-- Formulario -->
            <Frame BackgroundColor="White"
                   CornerRadius="10"
                   Padding="20"
                   HasShadow="True">

                <VerticalStackLayout Spacing="15">

                    <!-- Nombre y Apellidos -->
                    <Grid ColumnDefinitions="*,*,*"
                          ColumnSpacing="10">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="Nombre *"
                                   FontAttributes="Bold"/>
                            <Entry Placeholder="Nombre" Text="María" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1">
                            <Label Text="Apellido Paterno *"
                                   FontAttributes="Bold"/>
                            <Entry Placeholder="Apellido Paterno" Text="Gómez" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="2">
                            <Label Text="Apellido Materno" FontAttributes="Bold"/>
                            <Entry Placeholder="Apellido Materno" Text="López" />
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Correo y Rol -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="Correo Electrónico *"
                                   FontAttributes="Bold"/>
                            <Entry Placeholder="Correo" Keyboard="Email" Text="mgomez@example.com" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1">
                            <Label Text="Rol *" FontAttributes="Bold"/>
                            <Picker Title="Selecciona un rol" SelectedIndex="1">
                                <Picker.Items>
                                    <x:String>Estudiante</x:String>
                                    <x:String>Docente</x:String>
                                    <x:String>Administrador</x:String>
                                </Picker.Items>
                            </Picker>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Checkbox -->
                    <CheckBox IsChecked="True" />
                    <Label Text="Usuario activo" FontSize="14" />

                    <!-- Botones -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="15" Margin="0,20,0,0">
                        <Button Text="⬅ Volver al listado"
                                BackgroundColor="Gray"
                                TextColor="White"
                                CornerRadius="10"
                                Grid.Column="0"/>

                        <HorizontalStackLayout HorizontalOptions="End" Grid.Column="1" Spacing="10">
                            <Button Text="🗑 Eliminar"
                                    BackgroundColor="#d93025"
                                    TextColor="White"
                                    CornerRadius="10"/>
                            <Button Text="💾 Guardar Cambios"
                                    BackgroundColor="#1a73e8"
                                    TextColor="White"
                                    CornerRadius="10"/>
                        </HorizontalStackLayout>
                    </Grid>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
