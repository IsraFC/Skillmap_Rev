<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Skillmap.Pages.AsignacionesListadoPage"
             BackgroundColor="#f8f9fa"
             Title="Asignaciones">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="#1a73e8" CornerRadius="10" Padding="15">
                <VerticalStackLayout>
                    <Label Text="🔗 Asignación Recursos a Materias"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Relación entre recursos educativos y materias"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Barra de acciones -->
            <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                <Button Text="➕ Nueva Asignación"
                        BackgroundColor="#1a73e8"
                        TextColor="White"
                        CornerRadius="10"/>

                <Picker Title="Filtrar por materia..."
                        WidthRequest="250">
                    <Picker.Items>
                        <x:String>Programación Básica</x:String>
                        <x:String>Bases de Datos</x:String>
                        <x:String>Diseño Web</x:String>
                    </Picker.Items>
                </Picker>
            </HorizontalStackLayout>

            <!-- Tabla simulada -->
            <VerticalStackLayout Spacing="10">
                <!-- Registro 1 -->
                <Frame BackgroundColor="White" CornerRadius="10" BorderColor="#1a73e8" Padding="10">
                    <VerticalStackLayout>
                        <Label Text="ID Asignación: ASG-101" FontAttributes="Bold" TextColor="#1a73e8"/>
                        <Label Text="📘 Materia: MAT-101 | Programación Básica"/>
                        <Label Text="📎 Recurso: REC-205 | Guía HTML5"/>
                        <Label Text="Tipo: Documento PDF"/>
                        <Label Text="Fecha: 15/05/2025"/>
                        <Button Text="🗑️ Eliminar" BackgroundColor="#d93025" TextColor="White" CornerRadius="8" HeightRequest="35"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Registro 2 -->
                <Frame BackgroundColor="White" CornerRadius="10" BorderColor="#1a73e8" Padding="10">
                    <VerticalStackLayout>
                        <Label Text="ID Asignación: ASG-102" FontAttributes="Bold" TextColor="#1a73e8"/>
                        <Label Text="📘 Materia: MAT-205 | Bases de Datos"/>
                        <Label Text="📎 Recurso: REC-301 | Tutorial SQL"/>
                        <Label Text="Tipo: Video"/>
                        <Label Text="Fecha: 10/05/2025"/>
                        <Button Text="🗑️ Eliminar" BackgroundColor="#d93025" TextColor="White" CornerRadius="8" HeightRequest="35"/>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
