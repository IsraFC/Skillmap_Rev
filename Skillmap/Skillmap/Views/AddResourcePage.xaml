<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Skillmap.Views.AddResourcePage"
             Title="Nuevo Recurso Educativo"
             BackgroundColor="#f2f2f2"
             Shell.TabBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="25">

            <!-- Encabezado -->
            <Label Text="📚 Nuevo Recurso Educativo"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#0b8043"
                   HorizontalOptions="Center"/>

            <!-- Subtítulo -->
            <Label Text="Completa los datos obligatorios para registrar el recurso"
                   FontSize="14"
                   TextColor="Gray"
                   HorizontalOptions="Center"/>

            <Frame BackgroundColor="White" CornerRadius="15" Padding="20" HasShadow="True">

                <VerticalStackLayout Spacing="20">

                    <!-- Título -->
                    <Entry Text="{Binding Titulo}"
                           Placeholder="Título del recurso *"
                           FontSize="16"
                           BackgroundColor="#eeeeee"
                           TextColor="Black" />

                    <!-- Descripción -->
                    <Editor Text="{Binding Descripcion}"
                            Placeholder="Descripción del recurso *"
                            FontSize="16"
                            AutoSize="TextChanges"
                            HeightRequest="100"
                            BackgroundColor="#eeeeee"
                            TextColor="Black"/>

                    <!-- Link / Archivo -->
                    <Entry Text="{Binding Link}"
                           Placeholder="Enlace o ruta del recurso *"
                           FontSize="16"
                           BackgroundColor="#eeeeee"
                           TextColor="Black"/>

                    <!-- Pickers -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="Tipo de Recurso *" FontSize="14" FontAttributes="Bold"/>
                            <!-- Picker de Tipo -->
                            <Picker SelectedItem="{Binding TipoSeleccionado}"
                                    ItemsSource="{Binding Tipos}"
                                    ItemDisplayBinding="{Binding Id_Resource_Type}"
                                    Title="Seleccionar tipo..."
                                    BackgroundColor="#eeeeee"
                                    TextColor="Black"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="1">
                            <Label Text="Materia Asociada *" FontSize="14" FontAttributes="Bold"/>
                            <Picker SelectedItem="{Binding MateriaSeleccionada}"
                                    ItemsSource="{Binding Materias}"
                                    ItemDisplayBinding="{Binding Name}"
                                    Title="Seleccionar materia..."
                                    BackgroundColor="#eeeeee"
                                    TextColor="Black"/>
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Público -->
                    <HorizontalStackLayout>
                        <CheckBox IsChecked="{Binding EsPublico}"/>
                        <Label Text="Hacer recurso público" TextColor="Black" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <!-- Botón -->
                    <Button Text="💾 Guardar Recurso"
                            Command="{Binding GuardarRecursoCommand}"
                            BackgroundColor="#0b8043"
                            TextColor="White"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            HeightRequest="50"/>

                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
